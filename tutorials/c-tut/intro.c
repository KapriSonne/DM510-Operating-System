/*  include is the "preprocessor" directive which tells the compiler to put
    code from the header into our program before actually creating the executable.
*/
#include <stdio.h>

// our main function returns an integer in this case.
int main()
{
    printf( "blah.\n" );

    // Different variable types uses takes different size of memory
    // int uses
    int someVariable;

    // tells the operating system that the program ended with succes
    return 0;
}
